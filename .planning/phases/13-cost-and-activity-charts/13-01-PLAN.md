---
phase: 13-cost-and-activity-charts
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/insights/InsightsPanel.tsx
autonomous: true
requirements: [INSG-01, INSG-02, INSG-03, INSG-04]

must_haves:
  truths:
    - "InsightsPanel renders three tabs: Cost, Activity, Health"
    - "Cost tab shows the existing 'Cost by Model' and 'Cost by Session' bar charts (unchanged)"
    - "Activity tab renders a placeholder or empty state (charts added in Plan 03)"
    - "Health tab renders a placeholder ('Coming soon' or empty) reserved for Phase 14"
    - "Switching tabs does not crash or break existing functionality"
  artifacts:
    - path: "frontend/src/components/insights/InsightsPanel.tsx"
      provides: "Tabbed InsightsPanel with Cost/Activity/Health tabs; existing charts in Cost tab"
      contains: "CostTab, ActivityTab, HealthTab"
  key_links:
    - from: "InsightsPanel.tsx"
      to: "useObservStore"
      via: "existing costModels, sessionCosts usage unchanged inside CostTab"
      pattern: "useObservStore"
---

<objective>
Refactor InsightsPanel.tsx from a flat scrollable layout into a tabbed layout with three tabs: Cost, Activity, and Health. Move the two existing charts (Cost by Model, Cost by Session) and the Tool Call Latency widget into the Cost tab. Stub Activity and Health tabs as empty/placeholder — charts are added in Plans 02 and 03.

Purpose: Structural foundation that Plans 02 and 03 build on top of; establishes the tab layout locked by user decisions.
Output: InsightsPanel.tsx with working tab navigation, existing charts preserved in Cost tab, Activity and Health tabs stubbed.
</objective>

<execution_context>
@/Users/darshannere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darshannere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cost-and-activity-charts/13-CONTEXT.md

<interfaces>
<!-- Current InsightsPanel.tsx structure — executor should understand what's being refactored -->

Current file: frontend/src/components/insights/InsightsPanel.tsx
- Imports: BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer from 'recharts'
- Imports: useObservStore from '@/store/useObservStore'
- Imports: ToolEvent from '@/types'
- Exports: InsightsPanel() — flat scrollable div with three sections:
  1. "Cost by Model" BarChart (data: costModels → { name, cost })
  2. "Cost by Session (Top 10)" BarChart (data: sessionCosts → { name, cost })
  3. "Tool Call Latency" — p50/p95/count stat boxes (computed from events[])

Constants:
- TOOLTIP_STYLE: React.CSSProperties — reuse in all charts
- computeLatencyPercentiles(events): { p50, p95, count }

Established patterns (from 13-CONTEXT.md):
- Chart height: style={{ height: 160 }} with <ResponsiveContainer width="100%" height="100%">
- Tick style: tick={{ fontSize: 9, fill: '#6b7280' }}
- Cost formatter: tickFormatter={(v: number) => `$${v.toFixed(3)}`}
- Empty state: <p className="text-xs text-muted-foreground">No data yet</p>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor InsightsPanel into tabbed layout with Cost/Activity/Health tabs</name>
  <files>frontend/src/components/insights/InsightsPanel.tsx</files>
  <action>
Rewrite InsightsPanel.tsx with a three-tab layout. Use a simple local state (`useState`) to track the active tab — no external tab library required unless shadcn Tabs is already available; a plain button-based tab bar is acceptable.

Tab bar structure: Three tab buttons ("Cost", "Activity", "Health") rendered as a row at the top of the panel. Active tab button has a visible selected state (e.g., border-b-2 border-green-400 or similar, consistent with existing green/blue palette).

**Cost tab:** Contains all three existing content sections, unchanged:
1. "Cost by Model" BarChart (green bars, fill="#4ade80")
2. "Cost by Session (Top 10)" BarChart (blue bars, fill="#60a5fa")
3. "Tool Call Latency" stat boxes (p50/p95/count)
All existing data logic (useObservStore, useMemo, computeLatencyPercentiles) stays in place — just wrapped into the Cost tab render path.

**Activity tab:** Render an empty `<div>` or a brief placeholder paragraph: `<p className="text-xs text-muted-foreground">Charts loading...</p>`. This slot will be replaced in Plan 13-03.

**Health tab:** Render a placeholder: `<p className="text-xs text-muted-foreground">Health charts coming in next release.</p>` — per user decision to reserve this tab for Phase 14.

Keep TOOLTIP_STYLE constant and computeLatencyPercentiles helper at the top of the file (they remain needed by Cost tab).

Do NOT add any new API calls, new imports beyond what's already in the file, or new dependencies.
  </action>
  <verify>
    <automated>cd /Users/darshannere/claude/observagent/frontend && npx tsc --noEmit 2>&1 | grep -E "InsightsPanel|error TS" | head -20</automated>
  </verify>
  <done>InsightsPanel.tsx has three tabs; Cost tab renders all existing charts identically to before; Activity and Health tabs render placeholders; TypeScript compiles without errors in InsightsPanel.tsx</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` — no TypeScript errors
2. `npm run build` in frontend — build succeeds
3. Visually: Insights panel shows "Cost | Activity | Health" tab buttons; clicking Cost shows existing charts; clicking Activity and Health shows placeholder text
</verification>

<success_criteria>
- InsightsPanel renders tab bar with Cost, Activity, Health tabs
- Cost tab: all three existing chart sections present and functional
- Activity tab: renders without crash (placeholder content)
- Health tab: renders without crash (placeholder content)
- Zero new npm dependencies added
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-cost-and-activity-charts/13-01-SUMMARY.md`
</output>
