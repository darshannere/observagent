---
phase: 11-dashboard-overhaul-filters
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/store/useObservStore.ts
  - frontend/package.json
  - frontend/package-lock.json
autonomous: true
requirements: [DASH2-03]

must_haves:
  truths:
    - "ToolLog time filtering works by reading timeFilter from Zustand store"
    - "recharts is installed and importable in the frontend"
    - "setTimeFilter action updates the store and triggers ToolLog re-render"
  artifacts:
    - path: "frontend/src/store/useObservStore.ts"
      provides: "timeFilter state + setTimeFilter action"
      contains: "timeFilter: 'all'"
    - path: "frontend/package.json"
      provides: "recharts dependency"
      contains: "recharts"
  key_links:
    - from: "frontend/src/components/log/ToolLog.tsx"
      to: "useObservStore.timeFilter"
      via: "useObservStore((s) => s.timeFilter)"
      pattern: "timeFilter"
---

<objective>
Install recharts and extend the Zustand store with timeFilter state, enabling downstream plans to implement time-based filtering in ToolLog and the new Insights tab.

Purpose: Foundation for DASH2-03 (time filter buttons) and the Insights panel (recharts charts). Both Plans 02 and 03 depend on this plan's outputs.
Output: Updated useObservStore.ts with TimeFilter type + timeFilter state + setTimeFilter action; recharts installed in frontend.
</objective>

<execution_context>
@/Users/darshannere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darshannere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-dashboard-overhaul-filters/11-CONTEXT.md
@.planning/phases/11-dashboard-overhaul-filters/11-RESEARCH.md
</context>

<interfaces>
<!-- Current ObservStore shape (from frontend/src/store/useObservStore.ts) -->
<!-- Executor: add TimeFilter and timeFilter/setTimeFilter to the existing interface and create() call -->

interface ObservStore {
  // Existing state (do NOT remove)
  agents: Map<string, Agent>
  sessions: Map<string, Session>
  events: ToolEvent[]
  activeSessionFilter: string | null
  activeAgentFilter: string | null
  selectedAgent: string | null
  collapsedSessions: Set<string>
  sessionCosts: CostStateEntry[]
  todayCost: number
  costModels: ModelCost[]
  config: Config | null
  health: HealthState | null
  sseConnected: boolean
  contextFillPct: number

  // ADD THESE:
  timeFilter: TimeFilter

  // Existing actions (do NOT remove)
  setSelectedAgent(agentId: string | null): void
  toggleSessionCollapse(sessionId: string): void
  updateAgentCurrentTool(agentId: string, toolName: string): void
  // ... (many more existing actions)

  // ADD THIS ACTION:
  setTimeFilter(filter: TimeFilter): void
}
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <action>
    From the frontend/ directory, install recharts:

    ```bash
    cd /Users/darshannere/claude/observagent/frontend && npm install recharts
    ```

    Verify it installs without errors and appears in package.json dependencies.
    Note: recharts is SVG-based, TypeScript types are included in the package itself — no @types/recharts needed.
  </action>
  <verify>
    <automated>cd /Users/darshannere/claude/observagent/frontend && node -e "require.resolve('recharts')" && echo "recharts OK"</automated>
  </verify>
  <done>recharts appears in frontend/package.json dependencies and node_modules/recharts exists</done>
</task>

<task type="auto">
  <name>Task 2: Add TimeFilter type and timeFilter state to Zustand store</name>
  <files>frontend/src/store/useObservStore.ts</files>
  <action>
    Edit frontend/src/store/useObservStore.ts to add:

    1. Export the TimeFilter type near the top of the file (after imports, before ObservStore interface):
    ```typescript
    export type TimeFilter = '5m' | '15m' | '1h' | 'all'
    ```

    2. In the ObservStore interface, add after `contextFillPct: number`:
    ```typescript
    timeFilter: TimeFilter
    ```

    3. In the ObservStore interface actions section, add after the existing actions:
    ```typescript
    setTimeFilter(filter: TimeFilter): void
    ```

    4. In the create() initial state object, add after `contextFillPct: 0` (or wherever contextFillPct is initialized):
    ```typescript
    timeFilter: 'all',
    ```

    5. In the create() actions section, add the implementation:
    ```typescript
    setTimeFilter(filter) {
      set({ timeFilter: filter })
    },
    ```

    DO NOT remove or change any existing state, actions, or logic. This is a pure additive change.
    DO NOT add timeFilter to URL search params — it's ephemeral, store-only.
  </action>
  <verify>
    <automated>cd /Users/darshannere/claude/observagent/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>TypeScript build passes with no errors; useObservStore exports TimeFilter type, ObservStore interface includes timeFilter: TimeFilter and setTimeFilter action</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` in frontend/ exits with code 0
2. `grep -n "timeFilter\|setTimeFilter\|TimeFilter" frontend/src/store/useObservStore.ts` shows the type, state, and action
3. `grep "recharts" frontend/package.json` shows the dependency
</verification>

<success_criteria>
- recharts installed and available for import in frontend components
- TimeFilter type exported from useObservStore.ts
- timeFilter state initialized to 'all' in Zustand store
- setTimeFilter action present and functional
- TypeScript build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-overhaul-filters/11-01-SUMMARY.md`
</output>
