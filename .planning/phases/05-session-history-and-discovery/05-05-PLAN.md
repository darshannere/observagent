---
phase: 05-session-history-and-discovery
plan: 05
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-02
  - 05-03
  - 05-04
autonomous: false
files_modified: []
requirements:
  - HIST-01
  - HIST-02
  - HIST-03

must_haves:
  truths:
    - "User can see a list of past sessions at /history grouped by project"
    - "Project groups are collapsible and collapsed by default"
    - "Live sessions show LIVE badge and clicking it navigates to the main dashboard"
    - "Filters update the session list instantly as user types"
    - "Clicking a session card navigates to replay mode with a visible amber banner"
    - "Export JSONL and Export CSV produce downloadable files with correct filenames"
    - "All three requirements HIST-01, HIST-02, HIST-03 are verifiable by a human user"
  artifacts:
    - path: "public/history.html"
      provides: "History page visible at /history"
    - path: "public/index.html"
      provides: "Replay mode visible at /?session_id=X"
  key_links:
    - from: "history.html session card"
      to: "/?session_id=X replay mode"
      via: "card click navigates to /?session_id="
    - from: "replay banner export buttons"
      to: "/api/sessions/:id/export"
      via: "exportSession() fetch + blob download"
---

<objective>
Human verification of all Phase 5 deliverables. Confirm that HIST-01 (session list), HIST-02 (filtering), and HIST-03 (export) are all working from a user's perspective with real ObservAgent session data.

Purpose: Code verification cannot confirm visual layout, filter UX feel, or that downloaded files open correctly in a spreadsheet. Human eyes are required.
Output: Phase 5 marked complete, ready for Phase 6.
</objective>

<execution_context>
@/Users/darshannere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darshannere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 5 complete implementation:
- /history page with collapsible project groups, session cards, filter bar, LIVE badges
- Replay mode in existing dashboard (?session_id= param) with amber banner and export buttons
- Backend: /api/sessions (filtered list) and /api/sessions/:id/export endpoints
- project_name column in session_cost populated from JSONL cwd fields
  </what-built>
  <how-to-verify>
Start server: cd /Users/darshannere/claude/observagent && node server.js

**HIST-01: Session list organized by project**
1. Navigate to http://localhost:3000/history
2. Confirm: dark page loads, no JS errors in console
3. Confirm: sessions are grouped by project name under collapsible headers
4. Confirm: project groups are collapsed by default (click arrow/triangle to expand)
5. Confirm: each expanded group shows session cards with project name, date/time, total cost
6. If any active Claude Code session is running, confirm it appears with a green LIVE badge at the top of its project group
7. Click the LIVE badge — confirm it navigates to http://localhost:3000/ (main live dashboard)

**HIST-02: Filtering**
8. Type a project name in the Project filter input — confirm the session list updates within 1 second (no submit button needed)
9. Set a date range (From / To) — confirm sessions outside that range disappear
10. Click "More Filters" — confirm secondary filter row appears with cost range, model, and has-errors inputs
11. Check "Has Errors" — confirm only sessions that had errors are shown (or empty state if none)
12. Clear all filters — confirm full list returns

**HIST-01 + drill-down: Session replay**
13. Click a session card (not the LIVE badge, not an export button) — confirm navigation to http://localhost:3000/?session_id=SOME_ID
14. Confirm: amber replay banner appears at top of the page
15. Confirm: banner shows the session ID or project/date info
16. Confirm: "Back to History" link is visible and navigates back to /history
17. Confirm: the tool call log shows only events from that session (not live events)

**HIST-03: Export**
18. From the history list page, click "Export JSONL" on any session card
19. Confirm: a .jsonl file downloads with filename format observagent_[project]_[YYYY-MM-DD].jsonl
20. Open the file — confirm it contains one JSON object per line with tool_name, timestamp, duration_ms, exit_status fields
21. Click "Export CSV" on a session card
22. Confirm: a .csv file downloads with the correct filename
23. Open in a spreadsheet (or text editor) — confirm proper CSV with header row and data rows
24. Navigate to a session replay (step 13), then use the export buttons in the amber banner — confirm downloads work from replay mode too
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found (e.g., "filter not updating", "export 404", "groups not collapsing")</resume-signal>
</task>

</tasks>

<verification>
All three HIST requirements verified by human user:
- HIST-01: Sessions browsable at /history, grouped by project, collapsible
- HIST-02: Filters (date, project, cost, model, errors) update live
- HIST-03: JSONL and CSV exports download correctly with readable content
</verification>

<success_criteria>
- /history loads without errors and shows session list organized by project
- Project groups collapse/expand correctly
- All five filter types update results without page reload
- Session card navigation enters replay mode with amber banner
- Export produces valid, openable JSONL and CSV files
- Replay mode does not show live events from other sessions
</success_criteria>

<output>
After completion, create .planning/phases/05-session-history-and-discovery/05-05-SUMMARY.md
</output>
