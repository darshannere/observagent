---
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - frontend/src/components/AgentDetailPanel.tsx
  - frontend/src/components/AgentDetailTabs.tsx
autonomous: false
---

# Plan 10-04: AgentDetailPanel Component

## Requirements
- AGNT-05: User can click an agent row to open a per-agent detail panel
- AGNT-06: Agent detail panel shows the initial task description / prompt the agent was given when spawned
- AGNT-07: Agent detail panel shows context fill % bar (cumulative input tokens / model context window max)
- AGNT-08: Agent detail panel shows per-agent tool call history with timestamps
- AGNT-09: Agent detail panel shows input + output token counts per API call for that agent
- **AGNT-10: (DEFERRED to Phase 11)**: Conversation history - requires database schema, relay.py changes, and API endpoints

## Tasks

### Task 1: Create AgentDetailPanel slide-in component
- **File**: `frontend/src/components/AgentDetailPanel.tsx` (NEW)
- **Action**:
  1. Create slide-in side panel (320px wide, right side)
  2. Add header with agent name and close button
  3. Add tab bar with 4 tabs: Prompt, Context, Calls, Tokens
  4. Add conditional rendering based on selectedAgent from store
  5. Use CSS transition for slide-in animation
- **Verification**: Panel slides in from right when agent selected

### Task 2: Create PromptTab component
- **File**: `frontend/src/components/AgentDetailTabs.tsx` (NEW) or inline in AgentDetailPanel
- **Action**:
  1. Fetch agent detail from `/api/agents/:id/detail`
  2. Display initial_prompt in a scrollable container
  3. Show context fill % bar below prompt
  4. Handle loading and empty states
- **Verification**: Shows agent's initial task and context fill %

### Task 3: Create ContextTab component (DEFERRED to Phase 11)
- **File**: `frontend/src/components/AgentDetailTabs.tsx`
- **Action**:
  1. Display placeholder message: "Conversation history will be available in Phase 11"
  2. This feature is blocked by: database schema for message storage, relay.py parsing of subagent JSONL files, new API endpoints
  3. See Phase 11 roadmap for full implementation
- **Verification**: Shows informative placeholder (AGNT-10 deferred to Phase 11)

### Task 4: Create CallsTab component
- **File**: `frontend/src/components/AgentDetailTabs.tsx`
- **Action**:
  1. Display tool call history table with columns: Tool, Time, Duration, Status
  2. Fetch tool calls from `/api/agents/:id/detail`
  3. Format timestamps and durations
  4. Show exit status with color coding (green=0, red!=0)
- **Verification**: Table shows all tool calls for selected agent with timestamps

### Task 5: Create TokensTab component
- **File**: `frontend/src/components/AgentDetailTabs.tsx`
- **Action**:
  1. Display per-call token breakdown table: Time, Input, Output, Cache
  2. Fetch token data from `/api/agents/:id/detail`
  3. Show totals at bottom of table
  4. Format numbers with commas
- **Verification**: Table shows input/output/cache tokens per API call with totals

---

## Verification Criteria
- [ ] AgentDetailPanel renders as slide-in side panel
- [ ] Tab navigation works between all 4 tabs
- [ ] Prompt tab shows initial prompt and context fill %
- [ ] Context tab shows placeholder (AGNT-10 deferred to Phase 11)
- [ ] Calls tab shows tool call history with timestamps
- [ ] Tokens tab shows per-call token breakdown with totals

## must_haves
- [ ] AGNT-05: Panel opens when agent clicked
- [ ] AGNT-06: Prompt tab displays initial task
- [ ] AGNT-07: Context fill % bar visible in Prompt tab
- [ ] AGNT-08: Calls tab shows tool history
- [ ] AGNT-09: Tokens tab shows per-call breakdown
- [ ] AGNT-10: DEFERRED to Phase 11 (requires new database schema, relay.py changes, API endpoints)
