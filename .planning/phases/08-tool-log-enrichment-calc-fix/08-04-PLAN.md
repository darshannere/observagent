---
phase: 08-tool-log-enrichment-calc-fix
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/costEngine.js
  - public/index.html
autonomous: true
requirements:
  - CALC-01

must_haves:
  truths:
    - "The context fill % shown in the dashboard is closer to Claude Code's displayed value (was ~10% lower before)"
    - "An info icon appears next to the context fill % in the dashboard — hovering it shows an explanation of any residual difference"
    - "The cost panel display has no regressions — all existing fields still appear correctly"
  artifacts:
    - path: "lib/costEngine.js"
      provides: "getContextFillPercent() updated with AUTOCOMPACT_BUFFER subtraction (40K tokens) so the effective denominator is 160K for a 200K model"
      contains: "AUTOCOMPACT_BUFFER"
    - path: "public/index.html"
      provides: "Info icon with tooltip next to context fill % display explaining potential discrepancy"
      contains: "ObservAgent calculates context fill"
  key_links:
    - from: "lib/costEngine.js getContextFillPercent()"
      to: "effective denominator"
      via: "contextWindow - AUTOCOMPACT_BUFFER (if fix applied)"
      pattern: "effectiveWindow|AUTOCOMPACT_BUFFER"
    - from: "public/index.html context fill display"
      to: "info tooltip"
      via: "span.info-icon with title attribute"
      pattern: "info-icon.*ObservAgent"
---

<objective>
Investigate and resolve the ~10% context window fill % discrepancy between ObservAgent and Claude Code.

Purpose: CALC-01 must ship in Phase 8 so Phase 10's AGNT-07 (agent detail context fill bar) gets accurate values. This fix is independent of the tool enrichment pipeline.
Output: getContextFillPercent() adjusted to subtract autocompact buffer (or left unchanged with investigation notes), plus an info tooltip added to the dashboard regardless.
</objective>

<execution_context>
@/Users/darshannere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darshannere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From lib/costEngine.js lines 106-110 (current getContextFillPercent()):

  export function getContextFillPercent(model, lastUsage) {
    const totalInput = lastUsage.inputTokens + lastUsage.cacheReadTokens + lastUsage.cacheWrite5m + lastUsage.cacheWrite1h;
    const contextWindow = CONTEXT_WINDOWS[model] ?? CONTEXT_WINDOWS['_default'];
    return Math.min(100, Math.round((totalInput / contextWindow) * 100));
  }

From lib/costEngine.js lines 17-28 (CONTEXT_WINDOWS map — all 200_000):

  export const CONTEXT_WINDOWS = {
    'claude-sonnet-4-6':        200_000,
    'claude-opus-4-6':          200_000,
    // ... all models map to 200_000
    '_default':                 200_000,
  };

Research hypothesis: Claude Code reserves ~40,000 tokens as an autocompact buffer, using effectiveWindow = 200K - 40K = 160K as the denominator. This would cause ObservAgent to show ~10% LOWER % than Claude Code for the same token count.

From public/index.html — search for "ctx-pct" or "contextFillPct" to find the display element:
  The context fill % is displayed via an element with id="ctx-pct" or similar. The info icon goes next to this element.

Tooltip text to use (verbatim from CONTEXT.md):
  "ObservAgent calculates context fill from API token usage. Claude Code may apply internal scaling not exposed via the API."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate discrepancy and update getContextFillPercent()</name>
  <files>lib/costEngine.js</files>
  <action>
Investigation approach: Read the current costEngine.js formula carefully. The formula sums inputTokens + cacheReadTokens + cacheWrite5m + cacheWrite1h as totalInput, then divides by 200,000. Per Anthropic docs, this is correct for absolute token usage. Claude Code shows ~10% higher because it uses a smaller effective denominator: approximately 200,000 - 40,000 = 160,000.

Apply the autocompact buffer fix:

Add a constant before getContextFillPercent():
  // Claude Code reserves ~40K tokens as an autocompact buffer.
  // Using the same effective window makes ObservAgent's % match Claude Code's display.
  const AUTOCOMPACT_BUFFER = 40_000;

Update getContextFillPercent() to:
  export function getContextFillPercent(model, lastUsage) {
    const totalInput = lastUsage.inputTokens + lastUsage.cacheReadTokens + lastUsage.cacheWrite5m + lastUsage.cacheWrite1h;
    const contextWindow = CONTEXT_WINDOWS[model] ?? CONTEXT_WINDOWS['_default'];
    const effectiveWindow = contextWindow - AUTOCOMPACT_BUFFER;
    return Math.min(100, Math.round((totalInput / effectiveWindow) * 100));
  }

Note: If during implementation you find evidence that the discrepancy has a different root cause (e.g., Claude Code includes output tokens in its numerator), document it in a comment and apply whichever fix best matches the observed behavior. The goal is CALC-01: make the % match Claude Code or clearly document the difference.

If the fix cannot be determined without a live session comparison, apply the AUTOCOMPACT_BUFFER approach as the best-available estimate per research — it is consistent with the secondary source (codelynx.dev) that confirmed Claude Code uses an autocompact buffer.
  </action>
  <verify>node --input-type=module -e "
import { getContextFillPercent } from './lib/costEngine.js';
// With 80,000 input tokens on a 200K model, raw formula = 40%
// With autocompact buffer (effectiveWindow=160K), result = 50%
const pct = getContextFillPercent('claude-sonnet-4-6', {inputTokens:80000, cacheReadTokens:0, cacheWrite5m:0, cacheWrite1h:0});
console.assert(pct > 40, 'Expected > 40% with buffer applied, got ' + pct);
console.log('getContextFillPercent(80K tokens) =', pct + '% (should be 50% with 160K effective window)');
console.log('PASS');
"</verify>
  <done>
    - AUTOCOMPACT_BUFFER constant (40_000) is defined in costEngine.js
    - getContextFillPercent() uses effectiveWindow = contextWindow - AUTOCOMPACT_BUFFER as denominator
    - For 80K input tokens on 200K model: result is 50% (not 40%)
    - All existing token fields still summed the same way in numerator
  </done>
</task>

<task type="auto">
  <name>Task 2: Add info tooltip next to context fill % in dashboard</name>
  <files>public/index.html</files>
  <action>
Find the context fill % display element in index.html. Search for "ctx-pct" or "contextFill" or "fill %" to locate it. It will be an element like:
  <span id="ctx-pct">0%</span>

Add an info icon span immediately after the ctx-pct span:
  <span class="info-icon" title="ObservAgent calculates context fill from API token usage. Claude Code may apply internal scaling not exposed via the API.">ⓘ</span>

Add CSS for the info icon (place with other utility CSS, near the cost panel styles):
  .info-icon {
    cursor: help;
    font-size: 11px;
    color: var(--text-muted);
    margin-left: 2px;
    user-select: none;
  }

The title attribute provides a native browser tooltip — no JS required. The "ⓘ" character is U+24D8. This tooltip is added regardless of whether the formula fix was applied — it helps users understand the potential for small discrepancies even after the buffer fix.
  </action>
  <verify>grep -c "info-icon\|ObservAgent calculates" /Users/darshannere/claude/observagent/public/index.html</verify>
  <done>
    - .info-icon CSS class exists in index.html with cursor:help and color:var(--text-muted)
    - An element with class="info-icon" and the tooltip text appears next to the context fill % display
    - grep returns at least 2 matches (one for CSS definition, one for the HTML element)
  </done>
</task>

</tasks>

<verification>
1. node --input-type=module -e "import {getContextFillPercent} from './lib/costEngine.js'; console.log(getContextFillPercent('claude-sonnet-4-6', {inputTokens:80000,cacheReadTokens:0,cacheWrite5m:0,cacheWrite1h:0}));" from /Users/darshannere/claude/observagent — should print 50 (not 40)
2. grep -n "AUTOCOMPACT_BUFFER\|effectiveWindow" lib/costEngine.js — both terms must appear
3. grep -n "info-icon\|ObservAgent calculates" public/index.html — both must appear
</verification>

<success_criteria>
- getContextFillPercent() uses an effective window of 160K (200K minus 40K buffer) as denominator
- Context fill % now shows ~10% higher values, matching Claude Code's display more closely
- An info icon with tooltip appears next to the context fill % in the dashboard, explaining any residual discrepancy
- No regressions: cost panel still displays correctly, contextFillPct SSE events still trigger display updates
</success_criteria>

<output>
After completion, create /Users/darshannere/claude/observagent/.planning/phases/08-tool-log-enrichment-calc-fix/08-04-SUMMARY.md
</output>
