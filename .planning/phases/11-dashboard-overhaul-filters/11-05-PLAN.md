---
phase: 11-dashboard-overhaul-filters
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/HistoryPage.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FILT-01
  - DASH2-04
gap_closure: true

must_haves:
  truths:
    - "HistoryPage.tsx has a from/to date range picker allowing arbitrary date filtering of sessions"
    - "filteredSessions useMemo applies date range with priority over quick-filter buttons"
    - "Quick-filter buttons clear the date inputs when clicked; date inputs clear the active quick-filter when changed"
    - "REQUIREMENTS.md FILT-01 checkbox is checked and DASH2-04 checkbox is checked"
  artifacts:
    - path: "frontend/src/pages/HistoryPage.tsx"
      provides: "Date range picker UI (from/to date inputs) + filteredSessions useMemo with range priority"
      contains: "dateFrom, dateTo, HISTORY_WINDOW_MS"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement status"
      contains: "[x] FILT-01"
  key_links:
    - from: "dateFrom/dateTo inputs"
      to: "filteredSessions useMemo"
      via: "useState + useMemo deps"
      pattern: "dateFrom.*dateTo.*filteredSessions"
---

<objective>
Close FILT-01 gap: verify the date/time range picker implementation in HistoryPage.tsx is complete and correct, and fix stale requirement checkboxes in REQUIREMENTS.md.

Purpose: VERIFICATION.md flagged FILT-01 as undelivered (range picker absent). Commit 9b9abc8 added the range picker after the verification ran. This plan confirms the implementation is correct and marks the gap formally closed.

Output: Verified HistoryPage.tsx with date range picker, corrected REQUIREMENTS.md.
</objective>

<execution_context>
@/Users/darshannere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darshannere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-dashboard-overhaul-filters/11-VERIFICATION.md

# Gap context
# FILT-01 gap: date/time range picker was absent at verification time.
# Commit 9b9abc8 (fix: add date range picker to HistoryPage) landed after verification.
# Current HistoryPage.tsx (lines 155-293) already has:
#   - dateFrom / dateTo useState('')
#   - filteredSessions useMemo: dateFrom||dateTo branch takes priority over quick-filter window
#   - Two <input type="date"> fields with "or range:" label
#   - "x clear" button to reset both inputs back to quick-filter mode
#   - Quick-filter buttons call setDateFrom('') / setDateTo('') on click
</context>

<interfaces>
<!-- Key types and shapes in HistoryPage.tsx that the executor will verify against -->

```typescript
// FILT-01 implementation already present — executor must READ file first and confirm

type HistoryTimeFilter = '15m' | '1h' | '24h' | 'all'

// State
const [historyTimeFilter, setHistoryTimeFilter] = useState<HistoryTimeFilter>('all')
const [dateFrom, setDateFrom] = useState('')
const [dateTo, setDateTo] = useState('')

// filteredSessions priority logic (lines 183-197)
// if (dateFrom || dateTo) -> apply range filter
// else -> apply HISTORY_WINDOW_MS[historyTimeFilter] window

// UI rendered at lines 246-293 when sessions.length > 0:
// Quick buttons (clear dateFrom/dateTo on click)
// "or range:" label
// <input type="date"> dateFrom
// "→"
// <input type="date"> dateTo
// "x clear" button (shown when dateFrom || dateTo)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify and harden FILT-01 implementation in HistoryPage.tsx</name>
  <files>frontend/src/pages/HistoryPage.tsx</files>
  <action>
Read the current frontend/src/pages/HistoryPage.tsx in full.

Verify these four behaviors are correctly implemented:

1. **Date range state** — `dateFrom` and `dateTo` are `useState('')` at the top of `HistoryPage`.

2. **filteredSessions priority** — The useMemo checks `if (dateFrom || dateTo)` FIRST before the quick-filter window. When a date range is active, it parses `dateFrom` as `new Date(dateFrom).getTime()` (midnight start) and `dateTo` as `new Date(dateTo + 'T23:59:59').getTime()` (end of day). Sessions are kept where `last_event_ts` falls within `[fromMs, toMs]`. When neither date is set, fall through to the quick-filter `HISTORY_WINDOW_MS` lookup.

3. **UI cross-clearing** — Quick-filter button `onClick` calls `setDateFrom(''); setDateTo('')` in addition to `setHistoryTimeFilter(value)`. The date inputs' `onChange` call `setHistoryTimeFilter('all')` to deactivate the quick-filter highlight.

4. **Active state styling** — Quick-filter active class condition is `historyTimeFilter === value && !dateFrom && !dateTo` so the highlight disappears when a date range is active.

If any of these four points are missing or incorrect, fix them in-place with minimal surgical edits. Do NOT restructure the file.

If all four points are already correctly implemented (as expected from commit 9b9abc8), make no changes.

Run the TypeScript build to confirm zero errors:
```
cd /Users/darshannere/claude/observagent/frontend && npm run build 2>&1 | tail -5
```
  </action>
  <verify>
    <automated>cd /Users/darshannere/claude/observagent/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds with zero TS errors. HistoryPage.tsx has date range picker (dateFrom/dateTo inputs), useMemo priority logic, and cross-clearing between quick buttons and date inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale requirement checkboxes in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read .planning/REQUIREMENTS.md.

Fix two stale checkboxes:

1. **DASH2-04** (line ~89): Change `[ ] **DASH2-04**` to `[x] **DASH2-04**`. The context fill % bar is wired and functional per VERIFICATION.md (CostPanel reads contextFillPct from store; useSSE sets it on cost_update events; CALC-01 fix shipped in Phase 8). The checkbox was left unchecked by accident.

2. **FILT-01** (line ~93): Confirm it reads `[x] **FILT-01**`. If it is already checked, make no change. If unchecked, check it.

Make only these targeted checkbox changes. Do not alter any other text, formatting, or content.
  </action>
  <verify>
    <automated>grep -n "DASH2-04\|FILT-01" /Users/darshannere/claude/observagent/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Both DASH2-04 and FILT-01 lines show `[x]` checkboxes in REQUIREMENTS.md.</done>
</task>

</tasks>

<verification>
1. `npm run build` from `frontend/` exits 0 with no TypeScript errors
2. `grep -n "DASH2-04\|FILT-01" .planning/REQUIREMENTS.md` shows both as `[x]`
3. `grep -n "dateFrom\|dateTo\|HISTORY_WINDOW_MS" frontend/src/pages/HistoryPage.tsx` returns lines confirming state, useMemo, and UI all present
</verification>

<success_criteria>
- HistoryPage.tsx has working date range picker (from/to date inputs) that filters sessions to the specified range
- filteredSessions useMemo applies date range with priority over quick-filter buttons
- Quick-filter buttons deactivate their highlight when a date range is active and clear the inputs on click
- REQUIREMENTS.md: FILT-01 checked, DASH2-04 checked
- TypeScript build passes with zero errors
- Phase 11 FILT-01 gap is formally closed
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-overhaul-filters/11-05-SUMMARY.md` with:
- What was verified/fixed
- Final FILT-01 status
- Build output confirmation
</output>
