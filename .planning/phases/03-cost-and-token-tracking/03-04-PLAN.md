---
phase: 03-cost-and-token-tracking
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified: []
autonomous: false
requirements:
  - COST-01
  - COST-02
  - COST-03
  - COST-04

must_haves:
  truths:
    - "User has visually confirmed the cost panel shows real dollar cost and token data"
    - "User has confirmed the context fill bar changes color at 80%+"
    - "User has confirmed the budget alert banner appears when a threshold is set and exceeded"
    - "User has confirmed thresholds survive a server restart"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 3 cost and token tracking feature. Confirms all five success criteria from the roadmap are met before Phase 4 begins.

Purpose: Phase 4 (multi-agent observability) builds on Phase 3's cost infrastructure. A human-verified Phase 3 prevents broken foundations from propagating.
Output: Confirmed working cost panel with live updates, context fill bar, budget alert, and persistent thresholds.
</objective>

<execution_context>
@/Users/darshannere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darshannere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-cost-and-token-tracking/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human visual verification of Phase 3 cost panel</name>
  <files></files>
  <action>Human verifies the cost panel against all six checks listed in how-to-verify below.</action>
  <verify>Human types "approved" after confirming all six checks pass.</verify>
  <done>Human has visually confirmed all Phase 3 ROADMAP success criteria: real cost data shown, context fill bar visible with color coding, budget alert appears when threshold exceeded, thresholds persist across restart, live updates work, Phase 2 regression passes.</done>
  <what-built>
    Complete Phase 3 cost and token tracking feature:
    - lib/costEngine.js — PRICING map, dedup-safe usage extraction, cost formula
    - lib/jsonlWatcher.js — auto-discovers ~/.claude/projects/**/*.jsonl, watches for changes, upserts session_cost SQLite table, broadcasts cost_update SSE events
    - db/schema.js — session_cost and observagent_config tables added
    - routes/api.js — /api/cost and /api/config endpoints
    - server.js — startJsonlWatcher wired into startup
    - public/index.html — dedicated cost panel: session total, today's total, four token types, context fill bar, model breakdown, threshold inputs, alert banner
  </what-built>
  <how-to-verify>
    Start the server if not running:
    ```bash
    cd /Users/darshannere/claude/observagent && node server.js
    ```

    Open http://127.0.0.1:4999/ in your browser.

    **Check 1 — Cost panel visible (COST-01, COST-03):**
    - Cost panel shows with a session cost value (real data from your JSONL files)
    - Session cost uses $0.000000 format (6 decimal places, $ only)
    - All four token counts visible: In, Out, Cache↑, Cache↓
    - Today's total shows (may match session total if only one session today)

    **Check 2 — Context fill bar (COST-02):**
    - Context fill bar is visible with a fill percentage shown
    - Bar has a color: green if below 80%, yellow if 80-94%, red if 95%+
    - If currently below 80%, set ctx alert threshold to a value below the current fill % to confirm the warning color appears

    **Check 3 — Budget alert banner (COST-04):**
    - In the "Budget ($)" input, type a number lower than the current session cost (e.g. $0.01)
    - Confirm: a red alert banner appears at the top of the cost panel showing a specific message like "Session cost $X.XX exceeded budget $0.01" (not a generic warning)
    - Banner stays visible (does not fade or auto-dismiss)
    - Clear the budget threshold input — banner disappears

    **Check 4 — Threshold persistence:**
    - Set budget threshold to $5.00
    - Stop the server with Ctrl+C, then restart: `node server.js`
    - Reload http://127.0.0.1:4999/
    - Confirm: budget threshold input shows 5 (persisted across restart)

    **Check 5 — Live updates (COST-03 live aspect):**
    - With the dashboard open, trigger any Claude Code tool call in this session
    - Confirm: cost and token values in the panel update within ~2 seconds without page refresh

    **Check 6 — Phase 2 regression:**
    - Tool call log (Phase 2 panel) still shows live events when tool calls happen
    - No JavaScript errors in the browser console
  </how-to-verify>
  <resume-signal>
    Type "approved" if all six checks pass.
    Describe any issues observed (e.g. "cost shows $0.000000 despite active sessions", "alert banner not appearing") and the executor will create gap-closure plans.
  </resume-signal>
</task>

</tasks>

<verification>
Human-verified per checkpoint above.
</verification>

<success_criteria>
All six checks pass:
1. Cost panel shows real session cost in $0.000000 format with all four token types
2. Context fill bar is visible with correct color coding
3. Budget alert banner appears with specific message when threshold is exceeded; disappears when cleared
4. Threshold values persist across server restart
5. Cost panel updates live when new tool calls happen (no refresh required)
6. Phase 2 tool call log still works; no console errors
</success_criteria>

<output>
After verification approved, create `.planning/phases/03-cost-and-token-tracking/03-04-SUMMARY.md`
</output>
