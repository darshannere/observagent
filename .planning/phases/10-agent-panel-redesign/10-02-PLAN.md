---
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/index.ts
  - frontend/src/store/useObservStore.ts
  - frontend/src/hooks/useSSE.ts
autonomous: false
---

# Plan 10-02: Frontend Store & Types

## Requirements
- AGNT-04: Each agent row shows the current tool being executed in real time (live update on every PreToolUse event)
- AGNT-05: User can click an agent row to open a per-agent detail panel

## Tasks

### Task 1: Extend Agent interface with currentTool field
- **File**: `frontend/src/types/index.ts`
- **Action**: Add `currentTool: string | null` to the Agent interface
- **Verification**: TypeScript compiles without errors

### Task 2: Add selectedAgent and collapsedSessions to store
- **File**: `frontend/src/store/useObservStore.ts`
- **Action**: Add new state:
  - `selectedAgent: string | null` - currently selected agent ID
  - `collapsedSessions: Set<string>` - sessions with collapsed tree branches
- **Verification**: Store has new state with proper typing

### Task 3: Add store actions for selection and collapse
- **File**: `frontend/src/store/useObservStore.ts`
- **Action**: Add new actions:
  - `setSelectedAgent(agentId: string | null)` - select/deselect an agent
  - `toggleSessionCollapse(sessionId: string)` - toggle collapse state for a session
  - `updateAgentCurrentTool(agentId: string, toolName: string)` - update current tool for an agent
- **Verification**: All actions are callable and update state correctly

### Task 4: Handle currentTool in SSE hook
- **File**: `frontend/src/hooks/useSSE.ts`
- **Action**: In the PreToolUse handler, call `store.updateAgentCurrentTool` with the session_id and tool_name
- **Verification**: PreToolUse events trigger currentTool updates in store

### Task 5: Add active agent count getter
- **File**: `frontend/src/store/useObservStore.ts`
- **Action**: Add selector or getter to compute active agent count: `Array.from(agents.values()).filter(a => a.state === 'active').length`
- **Verification**: Returns correct count of active agents

---

## Verification Criteria
- [ ] Agent interface includes currentTool field
- [ ] Store has selectedAgent and collapsedSessions state
- [ ] All store actions work correctly
- [ ] SSE PreToolUse events update currentTool
- [ ] Active count computation is available

## must_haves
- [ ] AGNT-04: currentTool field exists and updates on PreToolUse
- [ ] AGNT-05: selectedAgent state available for detail panel
- [ ] AGNT-02: active agent count can be computed from store
